import { SearchpatientPipe } from './searchpatient.pipe';

describe('SearchpatientPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchpatientPipe();
    expect(pipe).toBeTruthy();
  });
});
